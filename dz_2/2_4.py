"""
5. Создать список, содержащий цены на товары (10–20 товаров), например:
[57.8, 46.51, 97, ...]

Вывести на экран эти цены через запятую в одну строку, цена должна отображаться в виде
<r> руб <kk> коп (например «5 руб 04 коп»).
Подумать, как из цены получить рубли и копейки, как добавить нули, если, например, получилось 7 копеек или 0 копеек
(должно быть 07 коп или 00 коп).
Вывести цены, отсортированные по возрастанию, новый список не создавать
(доказать, что объект списка после сортировки остался тот же).
Создать новый список, содержащий те же цены, но отсортированные по убыванию.
Вывести цены пяти самых дорогих товаров. Сможете ли вывести цены этих товаров по возрастанию, написав минимум кода?
"""

price_list = [57.8, 46.51, 97, 33.07, 4.04, 31, 77.38, 88.7, 32, 11.14, 95.3, 55, 73.07]

for price in price_list:
    print(f"{int(price)} руб {int((price % int(price))*100):0>2} коп")

print()
print(id(price_list))
print('\n')

price_list.sort()
for price in price_list:
    print(f"{int(price)} руб {int((price % int(price))*100):0>2} коп")

print()
print(id(price_list))
print('\n')

result_list = sorted(price_list, reverse=True)
for price in result_list[:5]:
    print(f"{int(price)} руб {int((price % int(price))*100):0>2} коп")

print()
print(id(result_list))
print('\n')